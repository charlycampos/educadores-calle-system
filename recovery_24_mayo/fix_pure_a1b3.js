const fs = require('fs');
const { execSync } = require('child_process');

const baselinePath = 'D:\\Usuarios\\ccampos\\.gemini\\antigravity-cli\\scratch\\NnaCreatePage_pure_a1b3.tsx';
const outputPath = 'D:\\Usuarios\\ccampos\\Documents\\Python Scripts\\Educadores_calle\\educadores-calle-system\\client\\src\\features\\nna\\NnaCreatePage.tsx';
const clientDir = 'D:\\Usuarios\\ccampos\\Documents\\Python Scripts\\Educadores_calle\\educadores-calle-system\\client';

console.log('Reading NnaCreatePage_pure_a1b3.tsx baseline file...');
let content = fs.readFileSync(baselinePath, 'utf8');

// Normalize line endings to LF to prevent CR/LF mismatches
content = content.replace(/\r\n/g, '\n');

const fixes = [
    {
        name: 'Fix 1 (Duplicate Apo properties in useForm default state)',
        target: `        autIdeEtEspApo: '',\n        tipoDiscapApo: '',\n        certDiscapApo: ''\n        autIdeEtApo: '',\n        autIdeEtEspApo: '',\n        tipoDiscapApo: '',\n        certDiscapApo: ''\n    });`,
        replacement: `        autIdeEtEspApo: '',\n        tipoDiscapApo: '',\n        certDiscapApo: ''\n    });`
    },
    {
        name: 'Fix 2 (Spliced abrirModalFamiliar and checkDuplicadoNna)',
        target: `    const abrirModalFamiliar = (idx?: number) => {\n        if (idx !== undefined) {\n            setFamiliarEditIdx(idx);\n            const raw = familiaresFields[idx] as any;\n            setFamiliarDraft({\n                nombres: raw.nombres || '',\n                parentesco: raw.parentesco || '',\n                dni: raw.dni || '',\n                telefono: raw.telefono || '',\n                ocupacion: raw.ocupacion || '',\n                viveCon: raw.viveCon || 'SI',\n                esTutor: !!raw.esTutor,\n                priApeTutApo: raw.priApeTutApo || '',\n                segApeTutApo: raw.segApeTutApo || '',\n                nomApeTutApo: raw.nomApeTutApo || '',\n                \n                // Guardar alertas en el estado\n                setNnaDuplicadoAlerta(prev => ({\n                    ...prev,\n                    [index]: json.hayDuplicados ? json.coincidencias : [],\n                }));\n\n                if (json.hayDuplicados && json.coincidencias.length > 0) {\n                    // Determinar si hay un duplicado exacto de DNI en las coincidencias\n                    const docBuscado = doc ? doc.toUpperCase().trim() : '';\n                    const esDniDuplicado = docBuscado && json.coincidencias.some(\n                        (c: any) => c.numeroDoc && String(c.numeroDoc).trim() === docBuscado\n                    );\n\n                    if (esDniDuplicado) {\n                        setNnaSemaforo(prev => ({ ...prev, [index]: 'error' }));\n                        \n                        // Si es error (DNI duplicado), auto-abrimos el Drawer de forma elegante\n                        const nombreCompleto = \`\${normStr(valores?.apellidoPaterno || '')} \${normStr(valores?.apellidoMaterno || '')} \${normStr(valores?.nombres || '')}\`.trim();\n                        setDuplicadosData(prev => {\n                            const filtrado = prev.filter(item => item.nnaIdx !== index);\n                            return [...filtrado, { nnaIdx: index, nombre: nombreCompleto, coincidencias: json.coincidencias }];\n                        });\n                        setDrawerNnaIdx(index);\n                        setDrawerOpen(true);\n                    } else {\n                        setNnaSemaforo(prev => ({ ...prev, [index]: 'warning' }));\n                    }\n                } else {\n                    setNnaSemaforo(prev => ({ ...prev, [index]: 'clean' }));\n                }\n            } else {\n                console.error('Error verificando duplicados:', await res.text());\n            }\n        } catch (err) {\n            console.error('Error de red al verificar duplicados:', err);\n        } finally {\n            setNnaBuscando(prev => ({ ...prev, [index]: false }));\n        }\n    };`,
        replacement: `    const abrirModalFamiliar = (idx?: number) => {\n        if (idx !== undefined) {\n            setFamiliarEditIdx(idx);\n            const raw = familiaresFields[idx] as any;\n            setFamiliarDraft({\n                nombres: raw.nombres || '',\n                parentesco: raw.parentesco || '',\n                dni: raw.dni || '',\n                telefono: raw.telefono || '',\n                ocupacion: raw.ocupacion || '',\n                viveCon: raw.viveCon || 'SI',\n                esTutor: !!raw.esTutor,\n                priApeTutApo: raw.priApeTutApo || '',\n                segApeTutApo: raw.segApeTutApo || '',\n                nomApeTutApo: raw.nomApeTutApo || '',\n                sexoApo: raw.sexoApo || '',\n                fechaNacApo: raw.fechaNacApo || '',\n                nacionalidadApo: raw.nacionalidadApo || 'PERUANA',\n                tipDocTutApo: raw.tipDocTutApo || '',\n                nroDocTutApo: raw.nroDocTutApo || '',\n                vinTutUsu: raw.vinTutUsu || '',\n                lenMatApo: raw.lenMatApo || '',\n                lenMatEspApo: raw.lenMatEspApo || '',\n                autIdeEtApo: raw.autIdeEtApo || '',\n                autIdeEtEspApo: raw.autIdeEtEspApo || '',\n                tipoDiscapApo: raw.tipoDiscapApo || '',\n                certDiscapApo: raw.certDiscapApo || ''\n            });\n        } else {\n            setFamiliarEditIdx(null);\n            setFamiliarDraft({\n                nombres: '',\n                parentesco: '',\n                dni: '',\n                telefono: '',\n                ocupacion: '',\n                viveCon: 'SI',\n                esTutor: false,\n                priApeTutApo: '',\n                segApeTutApo: '',\n                nomApeTutApo: '',\n                sexoApo: '',\n                fechaNacApo: '',\n                nacionalidadApo: 'PERUANA',\n                tipDocTutApo: '',\n                nroDocTutApo: '',\n                vinTutUsu: '',\n                lenMatApo: '',\n                lenMatEspApo: '',\n                autIdeEtApo: '',\n                autIdeEtEspApo: '',\n                tipoDiscapApo: '',\n                certDiscapApo: ''\n            });\n        }\n        setFamiliarModal(true);\n    };\n\n    const guardarFamiliar = () => {\n        let draft = { ...familiarDraft };\n        const current = watch('familiares') || [];\n\n        // Construir nombres a partir de los campos individuales\n        draft.nombres = \`\${draft.priApeTutApo || ''} \${draft.segApeTutApo || ''} \${draft.nomApeTutApo || ''}\`.trim() || draft.nombres;\n        // Sincronizar documento\n        draft.dni = draft.nroDocTutApo || draft.dni;\n        // Sincronizar parentesco\n        draft.parentesco = draft.vinTutUsu || draft.parentesco;\n\n        if (draft.esTutor) {\n            // Desmarcar cualquier otro familiar como tutor para asegurar unicidad\n            current.forEach((f: any, idx: number) => {\n                if (idx !== familiarEditIdx) {\n                    f.esTutor = false;\n                }\n            });\n        }\n\n        if (!draft.nombres.trim() || !draft.parentesco) return;\n\n        if (familiarEditIdx !== null) {\n            current[familiarEditIdx] = draft; \n            setValue('familiares', [...current]);\n        } else {\n            appendFamiliar(draft);\n        }\n        setFamiliarModal(false);\n    };\n\n    const normStr = (s: string): string => {\n        if (!s) return '';\n        const chars = s.normalize('NFD').split('');\n        const sinTildes = chars.filter(c => c.charCodeAt(0) < 0x0300 || c.charCodeAt(0) > 0x036F);\n        return sinTildes.join('').toUpperCase().trim();\n    };\n\n    const checkDuplicadoNna = async (index: number) => {\n        const todos = getValues('nnas') as any[];\n        const valores = todos?.[index];\n        if (!valores) return;\n\n        const ap      = normStr(valores.apellidoPaterno || '');\n        const nombres = normStr(valores.nombres         || '');\n        const doc     = (valores.numeroDoc || '').trim();\n        const tipoDoc =  valores.tipoDoc   || 'SIN_DOC';\n\n        const tieneNombre = ap && nombres;\n        const tieneDni    = doc && doc.length >= 6 && tipoDoc !== 'SIN_DOC';\n        \n        if (!tieneNombre && !tieneDni) {\n            setNnaSemaforo(prev => {\n                const copy = { ...prev };\n                delete copy[index];\n                return copy;\n            });\n            setNnaDuplicadoAlerta(prev => {\n                const copy = { ...prev };\n                delete copy[index];\n                return copy;\n            });\n            return;\n        }\n\n        const payload = {\n            nombres,\n            apellido_paterno: ap,\n            apellido_materno: normStr(valores.apellidoMaterno || '') || null,\n            numero_doc: tieneDni ? doc : null,\n            tipo_doc:   tipoDoc,\n        };\n\n        setNnaBuscando(prev => ({ ...prev, [index]: true }));\n        try {\n            const token = useAuthStore.getState().token || '';\n            const url = \`\${NNA_API_URL}/nna/verificar-duplicados\`;\n\n            const res = await fetch(url, {\n                method: 'POST',\n                headers: { 'Content-Type': 'application/json', Authorization: \`Bearer \${token}\` },\n                body: JSON.stringify(payload),\n            });\n\n            if (res.ok) {\n                const json = await res.json();\n                \n                // Guardar alertas en el estado\n                setNnaDuplicadoAlerta(prev => ({\n                    ...prev,\n                    [index]: json.hayDuplicados ? json.coincidencias : [],\n                }));\n\n                if (json.hayDuplicados && json.coincidencias.length > 0) {\n                    // Determinar si hay un duplicado exacto de DNI en las coincidencias\n                    const docBuscado = doc ? doc.toUpperCase().trim() : '';\n                    const esDniDuplicado = docBuscado && json.coincidencias.some(\n                        (c: any) => c.numeroDoc && String(c.numeroDoc).trim() === docBuscado\n                    );\n\n                    if (esDniDuplicado) {\n                        setNnaSemaforo(prev => ({ ...prev, [index]: 'error' }));\n                        \n                        // Si es error (DNI duplicado), auto-abrimos el Drawer de forma elegante\n                        const nombreCompleto = \`\${normStr(valores?.apellidoPaterno || '')} \${normStr(valores?.apellidoMaterno || '')} \${normStr(valores?.nombres || '')}\`.trim();\n                        setDuplicadosData(prev => {\n                            const filtrado = prev.filter(item => item.nnaIdx !== index);\n                            return [...filtrado, { nnaIdx: index, nombre: nombreCompleto, coincidencias: json.coincidencias }];\n                        });\n                        setDrawerNnaIdx(index);\n                        setDrawerOpen(true);\n                    } else {\n                        setNnaSemaforo(prev => ({ ...prev, [index]: 'warning' }));\n                    }\n                } else {\n                    setNnaSemaforo(prev => ({ ...prev, [index]: 'clean' }));\n                }\n            } else {\n                console.error('Error verificando duplicados:', await res.text());\n            }\n        } catch (err) {\n            console.error('Error de red al verificar duplicados:', err);\n        } finally {\n            setNnaBuscando(prev => ({ ...prev, [index]: false }));\n        }\n    };`
    },
    {
        name: 'Fix 3 (Spliced familiares load mapping)',
        target: `                    const mappedFamiliares = familiaresBackend.map((f: any) => {\n                        const parentescoVal = (() => {\n                            const clean = (f.parentesco || '').trim();\n                            if (clean === 'Padre' || clean === 'Madre') return '1: Padre o madre';\n                            if (clean === 'Tío/a' || clean === 'Tio/a' || clean === 'Tío' || clean === 'Tía') return '2: Tio/a';\n                            if (clean === 'Abuelo/a' || clean === 'Abuelo' || clean === 'Abuela') return '3: Abuelo/a';\n                            priApeTutApo: mainNna.priApeTutApo || '',\n                            segApeTutApo: mainNna.segApeTutApo || '',\n                            nomApeTutApo: mainNna.nomApeTutApo || '',\n                            sexoApo:      mainNna.sexoApo || '',\n                            fechaNacApo:  toDateInput(mainNna.fechaNacApo),\n                            nacionalidadApo: mainNna.nacionalidadApo || 'PERUANA',\n                            tipDocTutApo: mainNna.tipDocTutApo || '',\n                            nroDocTutApo: mainNna.nroDocTutApo || '',\n                            vinTutUsu:    mainNna.vinTutUsu || '',`,
        replacement: `                    const mappedFamiliares = familiaresBackend.map((f: any) => {\n                        const parentescoVal = (() => {\n                            const clean = (f.parentesco || '').trim();\n                            if (clean === 'Padre' || clean === 'Madre') return '1: Padre o madre';\n                            if (clean === 'Tío/a' || clean === 'Tio/a' || clean === 'Tío' || clean === 'Tía') return '2: Tio/a';\n                            if (clean === 'Abuelo/a' || clean === 'Abuelo' || clean === 'Abuela') return '3: Abuelo/a';\n                            if (clean === 'Hermano/a' || clean === 'Hermano' || clean === 'Hermana') return '4: Hermano/a';\n                            if (clean === 'Otro familiar') return '5: Otro familiar';\n                            return '';\n                        })();\n\n                        const isTutorMatch = mainNna.nombreTutor && f.nombres &&\n                            (mainNna.nombreTutor.toLowerCase().includes(f.nombres.toLowerCase()) ||\n                             f.nombres.toLowerCase().includes(mainNna.nombreTutor.toLowerCase()));\n\n                        if (isTutorMatch) {\n                            return {\n                                nombres:    f.nombres    || '',\n                                parentesco: parentescoVal,\n                                dni:        f.dni        || '',\n                                telefono:   f.telefono   || '',\n                                ocupacion:  f.ocupacion  || '',\n                                viveCon:    f.viveCon    || 'NO',\n                                esTutor:    true,\n                                priApeTutApo: mainNna.priApeTutApo || '',\n                                segApeTutApo: mainNna.segApeTutApo || '',\n                                nomApeTutApo: mainNna.nomApeTutApo || '',\n                                sexoApo:      mainNna.sexoApo || '',\n                                fechaNacApo:  toDateInput(mainNna.fechaNacApo),\n                                nacionalidadApo: mainNna.nacionalidadApo || 'PERUANA',\n                                tipDocTutApo: mainNna.tipDocTutApo || '',\n                                nroDocTutApo: mainNna.nroDocTutApo || '',\n                                vinTutUsu:    mainNna.vinTutUsu || '',\n                                lenMatApo:    normalizeLenMat(mainNna.lenMatApo),\n                                lenMatEspApo: mainNna.lenMatEspApo || '',\n                                autIdeEtApo:  normalizeEtnia(mainNna.autIdeEtApo),\n                                autIdeEtEspApo: mainNna.autIdeEtEspApo || '',\n                                tipoDiscapApo: normalizeTipoDiscap(mainNna.tipoDiscapApo),\n                                certDiscapApo: normalizeCertDiscap(mainNna.certDiscapApo),\n                            };\n                        }\n\n                        return {\n                            ...f,\n                            nombres:    f.nombres    || '',\n                            parentesco: parentescoVal,\n                            dni:        f.dni        || '',\n                            telefono:   f.telefono   || '',\n                            ocupacion:  f.ocupacion  || '',\n                            viveCon:    f.viveCon    || 'NO',\n                            esTutor:    false\n                        };\n                    });\n\n                    // Caso límite: si tiene tutor físico pero ningún familiar de la lista coincide, instanciamos un familiar tutor dinámico\n                    const hasTutorInList = mappedFamiliares.some((f: any) => f.esTutor);\n                    if (mainNna.tieneTutorApo === 1 && !hasTutorInList) {\n                        const nombreCompletoTutor = \`\${mainNna.priApeTutApo || ''} \${mainNna.segApeTutApo || ''} \${mainNna.nomApeTutApo || ''}\`.trim() || mainNna.nombreTutor || 'Tutor Principal';\n                        mappedFamiliares.push({\n                            nombres:      nombreCompletoTutor,\n                            parentesco:   mainNna.vinTutUsu || '1: Padre o madre',\n                            dni:          mainNna.nroDocTutApo || '',\n                            telefono:     mainNna.telefonoContacto || '',\n                            ocupacion:    '',\n                            viveCon:      'SI',\n                            esTutor:      true,\n                            priApeTutApo: mainNna.priApeTutApo || '',\n                            segApeTutApo: mainNna.segApeTutApo || '',\n                            nomApeTutApo: mainNna.nomApeTutApo || '',\n                            sexoApo:      mainNna.sexoApo || '',\n                            fechaNacApo:  toDateInput(mainNna.fechaNacApo),\n                            nacionalidadApo: mainNna.nacionalidadApo || 'PERUANA',\n                            tipDocTutApo: mainNna.tipDocTutApo || '',\n                            nroDocTutApo: mainNna.nroDocTutApo || '',\n                            vinTutUsu:    mainNna.vinTutUsu || '',\n                            lenMatApo:    mainNna.lenMatApo || '',\n                            lenMatEspApo: mainNna.lenMatEspApo || '',\n                            autIdeEtApo:  mainNna.autIdeEtApo || '',\n                            autIdeEtEspApo: mainNna.autIdeEtEspApo || '',\n                            tipoDiscapApo: mainNna.tipoDiscapApo || '',\n                            certDiscapApo: mainNna.certDiscapApo || '',\n                        });\n                    }\n\n                    return mappedFamiliares;\n                })(),`
    }
];

for (const fix of fixes) {
    if (content.includes(fix.target)) {
        console.log(`Applying ${fix.name}...`);
        content = content.replace(fix.target, fix.replacement);
    } else {
        console.log(`WARNING: ${fix.name} target NOT found in file!`);
    }
}

// Convert back to CRLF for Windows compatibility
content = content.replace(/\n/g, '\r\n');

fs.writeFileSync(outputPath, content, 'utf8');
console.log(`Saved fixed file to: ${outputPath}`);

console.log('Running direct tsc verification...');
try {
    execSync('npx tsc -p tsconfig.app.json --noEmit', { cwd: clientDir, stdio: 'pipe' });
    console.log('CONGRATULATIONS! FIXED BASELINE COMPILED WITH ZERO ERRORS!');
} catch (err) {
    const stderr = err.stderr ? err.stderr.toString() : '';
    const stdout = err.stdout ? err.stdout.toString() : '';
    const combined = stdout + '\n' + stderr;
    const errorLines = combined.split('\n').filter(l => l.includes('NnaCreatePage.tsx'));
    console.log(`FAILED with ${errorLines.length} errors.`);
    errorLines.slice(0, 30).forEach(l => console.log('  ' + l.trim()));
}
